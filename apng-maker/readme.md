Converts image files in 'frames' to .apng files by filename
frames MUST be named as 'filename_amount' e.g. 'EpicPicture_1', 'EpicPicture_2'
delay is measured in milliseconds

requirements:
	- apng: pip install apng